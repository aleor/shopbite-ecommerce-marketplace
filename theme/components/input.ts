import type { ComponentStyleConfig } from '@chakra-ui/theme';
import { colors } from '../foundations/colors';

export const Input: ComponentStyleConfig = {
  baseStyle: {
    errorBorderColor: colors.brand.red,
  },
};
